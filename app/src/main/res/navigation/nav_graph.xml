<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/welcomeFragment">
<!-- Setup navigation between fragment through buttons and bottom navigation bar - Stephen Orendain-->
    <fragment
        android:id="@+id/welcomeFragment"
        android:name="com.hfad.dev322projectdassh.WelcomeFragment"
        android:label="fragment_welcome"
        tools:layout="@layout/fragment_welcome" >
        <action
            android:id="@+id/action_welcomeFragment_to_timerFragment"
            app:destination="@id/timerFragment" />
        <!-- Action to navigate from welcome fragment to Results Fragment for Bottom Navigation Bar - Stephen Orendain-->
        <action
            android:id="@+id/action_timerFragment_to_resultsFragment"
            app:destination="@id/resultsFragment" />
    </fragment>

    <fragment
        android:id="@+id/timerFragment"
        android:name="com.hfad.dev322projectdassh.TimerFragment"
        android:label="fragment_timer"
        tools:layout="@layout/fragment_timer" >
        <action
            android:id="@+id/action_timerFragment_to_resultsFragment"
            app:destination="@id/resultsFragment" />
        <!-- Action to navigate from TimerFragment to WelcomeFragment for Bottom Navigation Bar - Stephen Orendain -->
        <action
            android:id="@+id/action_timerFragment_to_welcomeFragment"
            app:destination="@id/welcomeFragment" />
    </fragment>
    <fragment
        android:id="@+id/resultsFragment"
        android:name="com.hfad.dev322projectdassh.ResultsFragment"
        android:label="fragment_results"
        tools:layout="@layout/fragment_results" />
    <!-- Action to navigate from Results Fragment to WelcomeFragment for Bottom Navigation Bar - Stephen Orendain -->
    <action
        android:id="@+id/action_timerFragment_to_welcomeFragment"
        app:destination="@id/timerFragment" />

</navigation>
